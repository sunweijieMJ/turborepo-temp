{
  "name": "@repo/theme",
  "version": "0.0.0",
  "scripts": {
    "lint": "stylelint src --max-warnings 0",
    "build": "pnpm run clean && gulp",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@repo/stylelint-config": "workspace:*",
    "@types/gulp-sass": "^5.0.4",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.1.0",
    "sass": "^1.70.0"
  }
}
